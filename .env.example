# OpenAI API key (required for TTS and Whisper transcription)
OPENAI_API_KEY=sk-...

# Chrome executable path (optional — defaults to Playwright's bundled Chromium)
# CHROME_EXECUTABLE_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# Chrome user data directory for persistent Google login (optional)
# CHROME_USER_DATA_DIR=~/.gmeet-mcp/chrome-profile

# TTS voice (optional — defaults to "alloy")
# TTS_VOICE=alloy

# TTS model (optional — defaults to "gpt-4o-mini-tts")
# TTS_MODEL=gpt-4o-mini-tts

# Whisper model (optional — defaults to "whisper-1")
# WHISPER_MODEL=whisper-1

# Log level (optional — defaults to "info")
# LOG_LEVEL=info
